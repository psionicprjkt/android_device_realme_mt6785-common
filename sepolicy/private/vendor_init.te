# d2tw
get_prop(vendor_init, dt2w_prop)

# Allow init to write /proc/fastchg_fw_update
allow vendor_init proc_fastchg_fw:file rw_file_perms;

# Allow vendor_init to write touchpanel nodes
allow vendor_init proc_tp:file rw_file_perms;
